/*
#include "barreronde.h"
#include <math.h>
#include <iostream>


BarreRonde::BarreRonde(const string _reference,
                       const string _nom,
                       const double _longueur,
                       const double _densite,
                       const int _diametre):
    Barre(_reference, _nom, _longueur, _densite),
    diametre(_diametre)
 {
   cout << "Construction de la barre ronde ." << endl;
 }

 // Calcul de la section d'une barre ronde
 double BarreRonde::CalculerSection() {
     return (M_PI * pow(diametre, 2) / 4);
 }

 double BarreRonde::CalculerMasse() {
     return longueur * CalculerSection() * densite;
 }

 // Destructeur
 BarreRonde::~BarreRonde() {
     cout << "Destruction de la barre ronde." << endl;
 }
*/

